configuration:
  cloudflare_tunnel_token:
    name: Cloudflare tunnel token
    description: Optional manual cloudflared tunnel token. Setup Relay normally writes this automatically.
network:
  5000/tcp: LMS Edge Gateway for Home Assistant web UI
