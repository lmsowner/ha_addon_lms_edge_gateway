# Runtime app directory

The Docker build publishes `HA.LMS.EdgeGateway` into `/app` inside the add-on image.

This folder is kept only to document the intended runtime layout from the product specification. Build artifacts are not committed.
